{
  "routes": [
    {
      "method": "GET",
      "path": "/savings",
      "handler": "saving.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/savings/count",
      "handler": "saving.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/savings/:id",
      "handler": "saving.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/savings",
      "handler": "saving.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/savings/:id",
      "handler": "saving.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/savings/:id",
      "handler": "saving.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
